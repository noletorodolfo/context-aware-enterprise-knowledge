{
  "name": "@kb/sample-docs",
  "version": "0.1.0",
  "private": true,
  "description": "Converts the synthetic documents in samples/documents into .docx files for upload to SharePoint.",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "build:docx": "tsc -b && node dist/build.js"
  },
  "dependencies": {
    "docx": "^9.7.1"
  }
}
